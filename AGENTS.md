# Repository Instructions

- Git commit messages created by AI must follow the local rules in `AI_Git_Commit_Rules.md`.
- Use the required Conventional Commits format with an allowed English `type`, optional English `scope`, and a concise Chinese `subject`.
- Do not use vague commit messages such as `update`, `fix bug`, `wip`, or `修改代码`.
