# BitBond Web

BitBond Android WebView 宿主使用的 Vue 3 + Vite + TypeScript 前端工程。

## 脚本

- `npm run dev`：启动 Vite 本地开发服务器。
- `npm run build`：执行类型检查并构建静态应用。
- `npm run preview`：本地预览生产构建产物。
- `npm run test:smoke`：运行构建级冒烟检查。
